# Stage 0 — self-check

Answer without looking. If you can't, re-read the stage.

1. In one sentence: what *is* a container, technically?
2. Name the three Linux kernel features that make container isolation work, and what each provides.
3. What's the difference between an image and a container? Which one is mutable?
4. What does OCI stand for, and why does it matter that Kubernetes uses `containerd` instead of Docker?
5. Draw the layer stack of a container from memory. Which layer is writable? What happens to it on `docker rm`?
